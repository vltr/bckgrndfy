window.onload = function() {
    var demo = document.getElementById('demo');
    var canvas = generatePolyCanvas('demo', {
        width: 600,
        height: 300,
        cellSize: 45
    });
    // demo.appendChild(canvas);
}
